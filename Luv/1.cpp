#include <bits/stdc++.h> // Includes almost all Standard Library headers, useful for competitive programming
#include <iostream>      // Input and output operations
#include <algorithm>     // Functions for manipulating ranges (e.g., sort, search, transform)
#include <cmath>         // Mathematical functions and constants
#include <map>           // Associative arrays (ordered collections of key-value pairs)
#include <set>           // Collections of unique elements (ordered)
#include <vector>        // Dynamic arrays
#include <string>        // String manipulation
#include <unordered_map> // Hash tables (unordered collections of key-value pairs)
#include <unordered_set> // Collections of unique elements (unordered)
#include <queue>         // Queue data structure (FIFO)
#include <stack>         // Stack data structure (LIFO)
#include <list>          // Doubly linked lists (efficient insertions and deletions)
#include <deque>         // Double-ended queues (efficient modifications at both ends)
#include <bitset>        // Fixed-size arrays of bits (space optimization)
#include <climits>       // Definitions for integer limits (e.g., INT_MAX, INT_MIN)
#include <limits>        // Query properties of fundamental data types (e.g., min, max, precision)
#include <utility>       // Utility functions and types (e.g., std::pair, std::make_pair)
#include <ctime>         // Functions for manipulating date and time
#include <random>        // Facilities for generating random numbers
#include <functional>    // Functional programming utilities (e.g., function objects, binders)
#include <numeric>       // Numeric algorithms (e.g., std::accumulate, std::gcd)
#include <array>         // Fixed-size arrays
#include <tuple>         // Fixed-size collections of heterogeneous values
#include <fstream>       // File stream operations (e.g., std::ifstream, std::ofstream)
#include <sstream>       // String stream classes (e.g., std::istringstream, std::ostringstream)
#include <cassert>       // Debugging assertions (e.g., assert())
#include <iomanip>       // Input/output manipulators (e.g., setting precision, width, alignment)

using namespace std;
#define ll long long

void solve()
{
    cout << "Its working ,Alhamdulilah" << endl
         << endl;
    // Your solution logic here
}

int main()
{
    ll t = 1;
    //cin >> t;
    while (t--)
    {
        solve();
    }

    return 0;
}
